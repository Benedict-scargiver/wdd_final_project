{
  "name": "wdd_final_project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "start": "vite",
    "build": "npm install && vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .js"
  },
  "devDependencies": {
    "vite": "^6.1.0",
    "eslint": "^8.0.0"
  }
}
